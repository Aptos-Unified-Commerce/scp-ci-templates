name: "Agent: Build Docker Image & Push to ECR"

# Agents are Python services that depend on framework libraries from CodeArtifact.
# They are tested with uv, then built as Docker images and pushed to ECR.
# During Docker build, framework dependencies are pulled from CodeArtifact.

on:
  workflow_call:
    inputs:
      python-version:
        description: "Python version for testing"
        required: false
        type: string
        default: "3.11"
      aws-region:
        description: "AWS region for ECR and CodeArtifact"
        required: false
        type: string
        default: "us-east-1"
      ecr-repository:
        description: "ECR repository name for the agent image"
        required: true
        type: string
      dockerfile-path:
        description: "Path to Dockerfile"
        required: false
        type: string
        default: "Dockerfile"
      docker-context:
        description: "Docker build context directory"
        required: false
        type: string
        default: "."
      image-tag:
        description: "Additional image tag (besides git SHA, branch, latest)"
        required: false
        type: string
        default: ""
      # --- CodeArtifact (for pulling framework dependencies during docker build) ---
      codeartifact-domain:
        description: "CodeArtifact domain (for pulling framework libs)"
        required: true
        type: string
      codeartifact-repo:
        description: "CodeArtifact repository name"
        required: true
        type: string
      codeartifact-owner:
        description: "AWS account ID (CodeArtifact domain owner)"
        required: true
        type: string
      # --- Agent settings ---
      run-tests:
        description: "Whether to run tests before Docker build"
        required: false
        type: boolean
        default: true
      enable-healing:
        description: "Enable self-healing on failures"
        required: false
        type: boolean
        default: true
      enable-analysis:
        description: "Enable build history tracking"
        required: false
        type: boolean
        default: true
    secrets:
      AWS_ROLE_ARN:
        description: "IAM role ARN with ECR + CodeArtifact permissions"
        required: true
      ANTHROPIC_API_KEY:
        description: "Optional: Claude API key for AI-powered analysis"
        required: false

jobs:
  test:
    runs-on: ubuntu-latest
    if: inputs.run-tests

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Checkout CI templates
        uses: actions/checkout@v4
        with:
          repository: Aptos-Unified-Commerce/scp-ci-templates
          path: .ci-templates

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        run: uv python install ${{ inputs.python-version }}

      - name: Install CI Agent
        run: pip install .ci-templates/agent

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ inputs.aws-region }}

      # Configure uv/pip to pull framework libs from CodeArtifact
      - name: Configure CodeArtifact as package source
        run: |
          CODEARTIFACT_URL=$(aws codeartifact get-repository-endpoint \
            --domain ${{ inputs.codeartifact-domain }} \
            --domain-owner ${{ inputs.codeartifact-owner }} \
            --repository ${{ inputs.codeartifact-repo }} \
            --format pypi \
            --query repositoryEndpoint \
            --output text)
          CODEARTIFACT_TOKEN=$(aws codeartifact get-authorization-token \
            --domain ${{ inputs.codeartifact-domain }} \
            --domain-owner ${{ inputs.codeartifact-owner }} \
            --query authorizationToken \
            --output text)
          echo "UV_EXTRA_INDEX_URL=https://aws:${CODEARTIFACT_TOKEN}@${CODEARTIFACT_URL#https://}simple/" >> $GITHUB_ENV
          echo "PIP_EXTRA_INDEX_URL=https://aws:${CODEARTIFACT_TOKEN}@${CODEARTIFACT_URL#https://}simple/" >> $GITHUB_ENV
          echo "CODEARTIFACT_URL=$CODEARTIFACT_URL" >> $GITHUB_ENV
          echo "CODEARTIFACT_TOKEN=$CODEARTIFACT_TOKEN" >> $GITHUB_ENV

      - name: Install dependencies (including framework libs from CodeArtifact)
        run: uv sync --all-extras

      # ---- Primary test run ----
      - name: Run tests
        id: test
        continue-on-error: true
        run: |
          uv run pytest 2>&1 | tee /tmp/build.log

      # ---- Self-healing ----
      - name: Diagnose test failure
        id: heal
        if: inputs.enable-healing && steps.test.outcome == 'failure'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: ci-agent heal --log-file /tmp/build.log --attempt 1

      - name: Apply healing
        if: steps.heal.outcome == 'success' && steps.heal.outputs.should-retry == 'true'
        run: |
          COMMANDS='${{ steps.heal.outputs.retry-commands }}'
          if [ -n "$COMMANDS" ]; then
            echo "$COMMANDS" | while IFS= read -r cmd; do
              [ -n "$cmd" ] && eval "$cmd"
            done
          fi

      - name: Retry tests
        id: retry
        if: steps.heal.outcome == 'success' && steps.heal.outputs.should-retry == 'true'
        run: |
          uv sync --all-extras
          uv run pytest

      - name: Fail if tests failed
        if: always()
        run: |
          if [ "${{ steps.test.outcome }}" = "success" ] || [ "${{ steps.retry.outcome }}" = "success" ]; then
            echo "Agent tests passed"
          else
            echo "::error::Agent tests failed after healing attempts"
            exit 1
          fi

  docker-build-push:
    needs: test
    if: ${{ !cancelled() && (needs.test.result == 'success' || needs.test.result == 'skipped') }}
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Checkout CI templates
        uses: actions/checkout@v4
        with:
          repository: Aptos-Unified-Commerce/scp-ci-templates
          path: .ci-templates

      - name: Install CI Agent
        run: pip install .ci-templates/agent

      - name: Restore build history
        if: inputs.enable-analysis
        uses: actions/cache@v4
        with:
          path: build_history.json
          key: build-history-${{ github.repository }}-${{ github.ref_name }}
          restore-keys: |
            build-history-${{ github.repository }}-

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ inputs.aws-region }}

      # Generate CodeArtifact pip index URL for Docker build
      - name: Get CodeArtifact credentials for Docker build
        id: codeartifact
        run: |
          CODEARTIFACT_URL=$(aws codeartifact get-repository-endpoint \
            --domain ${{ inputs.codeartifact-domain }} \
            --domain-owner ${{ inputs.codeartifact-owner }} \
            --repository ${{ inputs.codeartifact-repo }} \
            --format pypi \
            --query repositoryEndpoint \
            --output text)
          CODEARTIFACT_TOKEN=$(aws codeartifact get-authorization-token \
            --domain ${{ inputs.codeartifact-domain }} \
            --domain-owner ${{ inputs.codeartifact-owner }} \
            --query authorizationToken \
            --output text)
          PIP_INDEX="https://aws:${CODEARTIFACT_TOKEN}@${CODEARTIFACT_URL#https://}simple/"
          echo "pip-index-url=$PIP_INDEX" >> "$GITHUB_OUTPUT"

      - name: Login to Amazon ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v2

      - name: Extract metadata
        id: meta
        run: |
          REGISTRY=${{ steps.ecr-login.outputs.registry }}
          REPO=${{ inputs.ecr-repository }}
          SHA_SHORT=$(git rev-parse --short HEAD)
          BRANCH=$(echo "${GITHUB_REF_NAME}" | sed 's/[^a-zA-Z0-9._-]/-/g')

          TAGS="${REGISTRY}/${REPO}:${SHA_SHORT}"
          TAGS="${TAGS},${REGISTRY}/${REPO}:${BRANCH}"

          if [ "${{ github.ref }}" = "refs/heads/main" ]; then
            TAGS="${TAGS},${REGISTRY}/${REPO}:latest"
          fi

          if [ -n "${{ inputs.image-tag }}" ]; then
            TAGS="${TAGS},${REGISTRY}/${REPO}:${{ inputs.image-tag }}"
          fi

          echo "tags=$TAGS" >> "$GITHUB_OUTPUT"
          echo "primary-image=${REGISTRY}/${REPO}:${SHA_SHORT}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ---- Docker build — pass CodeArtifact index so Dockerfile can pip install framework libs ----
      - name: Build and push agent image
        id: docker-build
        continue-on-error: true
        uses: docker/build-push-action@v6
        with:
          context: ${{ inputs.docker-context }}
          file: ${{ inputs.dockerfile-path }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            PIP_EXTRA_INDEX_URL=${{ steps.codeartifact.outputs.pip-index-url }}

      # ---- Self-healing for Docker build ----
      - name: Diagnose Docker failure
        id: heal-docker
        if: inputs.enable-healing && steps.docker-build.outcome == 'failure'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "Docker build failed" > /tmp/docker-build.log
          ci-agent heal --log-file /tmp/docker-build.log --attempt 1

      - name: Retry Docker build (no cache)
        id: retry-docker
        if: steps.heal-docker.outcome == 'success' && steps.heal-docker.outputs.should-retry == 'true'
        uses: docker/build-push-action@v6
        with:
          context: ${{ inputs.docker-context }}
          file: ${{ inputs.dockerfile-path }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          no-cache: true
          build-args: |
            PIP_EXTRA_INDEX_URL=${{ steps.codeartifact.outputs.pip-index-url }}

      # ---- Record and report ----
      - name: Determine status
        id: status
        if: always()
        run: |
          if [ "${{ steps.docker-build.outcome }}" = "success" ]; then
            echo "status=success" >> "$GITHUB_OUTPUT"
          elif [ "${{ steps.retry-docker.outcome }}" = "success" ]; then
            echo "status=healed" >> "$GITHUB_OUTPUT"
          else
            echo "status=failure" >> "$GITHUB_OUTPUT"
          fi

      - name: Record build result
        if: always() && inputs.enable-analysis
        run: |
          ci-agent record \
            --build-type agent \
            --status "${{ steps.status.outputs.status }}" \
            --failure-class "${{ steps.heal-docker.outputs.failure-class || '' }}" \
            --healing-strategy "${{ steps.heal-docker.outputs.strategy || '' }}"

      - name: Save build history
        if: always() && inputs.enable-analysis
        uses: actions/cache/save@v4
        with:
          path: build_history.json
          key: build-history-${{ github.repository }}-${{ github.ref_name }}-${{ github.run_id }}

      - name: Summary
        if: always()
        run: |
          STATUS="${{ steps.status.outputs.status }}"
          echo "### Agent Image — ${STATUS}" >> $GITHUB_STEP_SUMMARY
          if [ "$STATUS" != "failure" ]; then
            echo "- **Image:** ${{ steps.meta.outputs.primary-image }}" >> $GITHUB_STEP_SUMMARY
            echo "- **Tags:** ${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Fail if not healed
        if: always()
        run: |
          if [ "${{ steps.status.outputs.status }}" = "failure" ]; then
            exit 1
          fi
